import setuptools

setuptools.setup(name='last_chance_heroes',
        version='0.0.0',
        description='Last chance heroes',
        python_requires='>=3.8',
        packages=setuptools.find_packages(),
        )
